# BAB-002 Unblock Handoff

**From:** G-Tard (Mac webchat) — session `cd16f62e`
**To:** WhatsApp group (`agent:main:whatsapp:group:120363425074133557@g.us`) / G-Tard Lin / Retard
**Date:** 2026-05-06 23:27 EDT
**Status:** BAB-002 Studio code verified ✅ | DataStore API permission needed 📋

---

## Summary

Lin's BAB-002 implementation (commit `48d9f8f`) is **code-complete and Studio-verified**. All 4 scripts synced and syntax-checked. Ready for Play test.

**One human action blocks progress:** Enable "Studio Access to API Services" in Roblox Studio Game Settings so DataStore calls can execute in Play mode.

---

## What I did

1. **Installed toolchain** (Mac was missing rokit + rojo)
   - `rokit 1.2.0` installed via official script
   - `rokit install` ran successfully → `rojo 7.6.1` + `mantle 0.11.18` wired
   - Verified: `rojo --version` = Rojo 7.6.1 ✅

2. **Built BAB-002 rbxlx**
   - `./scripts/build.sh` → `BloomAndBurgle.rbxlx` 96K ✅

3. **Synced 4 BAB-002 scripts to Studio**
   - DataStore.luau (ModuleScript)
   - PlantHandler.server.luau (Script)
   - PlotManager.server.luau (Script)
   - LeaderstatsScript.server.luau (Script)
   - All synced via `./scripts/push-to-studio.sh` ✅

4. **Verified code syntax in Studio**
   - `script_grep` found `_G.PlantHandler = API` at PlantHandler line 317 ✅
   - `script_read` DataStore lines 1-80 show new DEFAULT_DATA fields ✅
     - `plotSlotIndex = nil`
     - `starterPackGranted = false`
     - `plot = { planters = {}, stash = {} }`
   - No compile errors in any of the 4 scripts ✅

---

## What's needed

### Human ask (Retard)

Open Roblox Studio → Game Settings (gear icon, top menu) → Security tab → find "Studio Access to API Services" → toggle it ON

**Why:** Studio is denying DataStore read/write with `StudioAccessToApisNotAllowed`. This is a Studio security setting, not a code bug.

**Confirmation:** After enabling, start a Play session and check console for DataStore success logs instead of the API rejection.

---

## Smoke test procedure (once API enabled)

1. Click Play in Studio
2. Player joins; LeaderstatsScript runs; DataStore.loadData() executes
3. Open SeedShop, buy a seed, plant in an empty plot slot
4. Watch plant grow (visual sprout animates)
5. Harvest → stash increments by 1
6. Rejoin session (kick from game, rejoin) → plant is still there at same grow stage, stash still has the harvested count
7. Server-cycle test: `:Shutdown()` or kick all players, rejoin → plant + stash persists
8. AFK test: Edit seed's `growSeconds` to 1 second (for testing), plant, wait 2s, rejoin → seed is ripe on load (harvest immediately)

---

## Commit reference

- `082f352` — chore(coord): BAB-002 Studio code verified — toolchain unblocked, all scripts synced, awaiting DataStore API enable
- STATUS.md updated to reflect current state

---

## Next steps after API enable

1. Run smoke test above
2. If all 8 steps pass → Mark BAB-002 as **DONE** (close ticket, merge sign-off from Lin + verification from Mac)
3. If any step fails → File a child ticket (BAB-002.1, etc.) with the specific failure

